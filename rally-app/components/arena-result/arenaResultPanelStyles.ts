import { StyleSheet } from 'react-native'

import { Radius, Spacing, type SportPalette } from '@/constants/theme'

export function createArenaResultPanelStyles(theme: SportPalette) {
  return StyleSheet.create({
    root: {
      flex: 1,
      backgroundColor: theme.arenaPage,
    },
    content: {
      gap: Spacing.lg,
      paddingHorizontal: Spacing.lg,
      paddingBottom: Spacing.xxxl,
    },
    loading: {
      flex: 1,
      alignItems: 'center',
      justifyContent: 'center',
      gap: Spacing.sm,
      paddingHorizontal: Spacing.xl,
      backgroundColor: theme.arenaPage,
    },
    loadingText: { color: theme.muted, fontSize: 14, fontWeight: '800', textAlign: 'center' },
    header: {
      flexDirection: 'row',
      alignItems: 'center',
      gap: Spacing.md,
    },
    activityIcon: {
      width: 48,
      height: 48,
      borderRadius: Radius.xl,
      alignItems: 'center',
      justifyContent: 'center',
    },
    headerCopy: { flex: 1, minWidth: 0, gap: Spacing.xs },
    eyebrow: {
      color: theme.muted,
      fontSize: 11,
      fontWeight: '900',
      letterSpacing: 1.1,
    },
    title: {
      color: theme.ink,
      fontSize: 26,
      fontWeight: '900',
      fontStyle: 'italic',
    },
    staticStatus: {
      minHeight: 30,
      borderRadius: Radius.pill,
      backgroundColor: theme.surface,
      borderWidth: 1,
      borderColor: theme.line,
      alignItems: 'center',
      justifyContent: 'center',
      paddingHorizontal: Spacing.sm,
    },
    staticStatusText: { color: theme.muted, fontSize: 11, fontWeight: '900' },
    reconnecting: {
      minHeight: 40,
      borderRadius: Radius.lg,
      borderWidth: 1,
      borderColor: theme.line,
      backgroundColor: theme.surface,
      flexDirection: 'row',
      alignItems: 'center',
      gap: Spacing.sm,
      paddingHorizontal: Spacing.md,
    },
    reconnectingText: { color: theme.muted, fontSize: 13, fontWeight: '800', flex: 1 },
    notice: {
      borderRadius: Radius.xl,
      borderWidth: 1,
      borderColor: theme.risk,
      backgroundColor: theme.riskSoft,
      gap: Spacing.sm,
      padding: Spacing.lg,
    },
    noticeRow: { flexDirection: 'row', alignItems: 'center', gap: Spacing.sm },
    noticeTitle: { color: theme.risk, fontSize: 15, fontWeight: '900', flex: 1 },
    noticeDetail: { color: theme.inkSoft, fontSize: 13, fontWeight: '700' },
    primaryAction: {
      minHeight: 52,
      borderRadius: Radius.pill,
      backgroundColor: theme.orange,
      flexDirection: 'row',
      alignItems: 'center',
      justifyContent: 'center',
      gap: Spacing.sm,
      paddingHorizontal: Spacing.lg,
    },
    primaryActionDisabled: { backgroundColor: theme.surfaceStrong, borderWidth: 1, borderColor: theme.line },
    primaryActionText: { color: theme.bg, fontSize: 14, fontWeight: '900' },
    primaryActionTextDisabled: { color: theme.muted },
    helper: { color: theme.muted, fontSize: 13, fontWeight: '800', textAlign: 'center' },
    standaloneActions: { flexDirection: 'row', flexWrap: 'wrap', gap: Spacing.sm },
    secondaryAction: {
      flexGrow: 1,
      minHeight: 48,
      borderRadius: Radius.pill,
      borderWidth: 1,
      borderColor: theme.line,
      backgroundColor: theme.surface,
      flexDirection: 'row',
      alignItems: 'center',
      justifyContent: 'center',
      gap: Spacing.xs,
      paddingHorizontal: Spacing.md,
    },
    secondaryActionText: { color: theme.inkSoft, fontSize: 13, fontWeight: '900' },
    dangerAction: { borderColor: theme.risk, backgroundColor: theme.riskSoft },
    dangerActionText: { color: theme.risk, fontSize: 13, fontWeight: '900' },
    terminal: {
      borderRadius: Radius.xxl,
      borderWidth: 1,
      borderColor: theme.line,
      backgroundColor: theme.bgElevated,
      alignItems: 'center',
      gap: Spacing.md,
      padding: Spacing.xl,
    },
    terminalIcon: {
      width: 52,
      height: 52,
      borderRadius: Radius.xl,
      alignItems: 'center',
      justifyContent: 'center',
    },
    terminalTitle: { color: theme.ink, fontSize: 24, fontWeight: '900', fontStyle: 'italic', textAlign: 'center' },
    terminalDetail: { color: theme.muted, fontSize: 14, fontWeight: '800', textAlign: 'center' },
    finalScore: { color: theme.ink, fontSize: 42, fontWeight: '900', fontStyle: 'italic', fontVariant: ['tabular-nums'] },
    finalDash: { color: theme.muted },
    rotation: { color: theme.green, fontSize: 13, fontWeight: '900', textAlign: 'center' },
    returnAction: {
      minHeight: 52,
      alignSelf: 'stretch',
      borderRadius: Radius.pill,
      borderWidth: 1,
      borderColor: theme.line,
      backgroundColor: theme.surface,
      flexDirection: 'row',
      alignItems: 'center',
      justifyContent: 'center',
      gap: Spacing.sm,
      paddingHorizontal: Spacing.lg,
    },
    returnActionText: { color: theme.ink, fontSize: 14, fontWeight: '900' },
  })
}
